## 목표
- 개인적으로 사용할 월별/예산기간별 예산관리 모바일앱 개발
- 카테고리별 예산을 설정하고 해당 예산 안에서 돈을 사용하도록 관리하는 것
- 자산관리, 카드사 지출내역 연동 이런거 없이 수기로 직접 사용한 내역을 입력하면서 돈을 썼음을 한 번 더 자각하기
- 지출 내역을 입력하면 예산에서 차감하고, 월간/연간 지출 보고서는 최대한 자동으로 제공

## 요구사항

### 지출/수입 입력
- 실제 결제나 지출이 있을 때마다 수기로 지출 카테고리, 금액, 결제방법을 입력 혹은 선택하고 날짜는 기본적으로 오늘 날짜가 나오되 수정이 가능해야 한다.
- 매번 수기로 입력해야 하므로 입력이 최대한 간편해야 하고 자주 입력하는 건 바로 불러와서 입력할 수 있어야 한다.
- 현금과 카드지출 모두 카드 대금 출금일이나 카드 결제일과 무관하게 사용자가 결제한 순간을 기준으로 바로 알맞은 카테고리 예산에 반영된다.
- 자주 입력하는 항목은 최근 입력 내역을 불러와 재사용할 수 있어야 한다.
- 사용자가 반복적으로 쓰는 항목은 즐겨찾기/템플릿으로 저장해 빠르게 입력할 수 있어야 한다.
- 금액은 KRW 정수 단위로 입력하며, 일반 지출/수입 입력에서는 음수 금액을 허용하지 않는다.
- 결제 취소나 환불은 사용자가 양수 금액으로 입력하면 시스템이 마이너스 지출로 저장한다. 사용자는 원 지출을 선택해 환불/취소 내역과 연결할 수 있어야 하며, 원 지출을 찾지 못한 경우에는 연결 없이 저장할 수 있어야 한다.
- 입력 오류는 사용자가 확인한 뒤 해당 지출/수입 내역을 삭제할 수 있어야 한다. 화면에서는 삭제로 보이되 내부적으로는 `deletedAt`을 남기는 소프트 삭제로 처리한다.
- 삭제된 지출/수입 내역은 삭제일 기준 2개월이 지나면 내부 데이터에서도 완전히 삭제한다.
- 수입 입력 메뉴가 있어서 어떤 통장계좌 혹은 현금 수입에 대한 금액을 입력할 수 있고 이를 통장계좌 또는 현금 계좌 잔액에 자동으로 반영해야 한다.
- 수입 입력 날짜도 기본적으로 오늘 날짜가 나오되 수정이 가능해야 한다.

### 예산/카테고리
- 사용자는 예산기간별 카테고리 예산을 설정할 수 있어야 한다.
- 월 예산은 예산기간 단위 스냅샷으로 관리한다. 카테고리 예산을 나중에 변경하더라도 과거 기간의 보고서가 바뀌지 않도록 `예산기간 id + 카테고리` 기준 예산 금액을 별도로 보존한다.
- 예산기간은 시작일과 종료일을 가진다. 월 시작일은 기본적으로 매월 1일로 두되, 사용자가 급여일 기준 예산 기간을 사용할 수 있도록 설정 가능해야 한다.
- 새 예산기간을 만들 때는 이전 예산기간의 카테고리별 예산을 복사해 시작할 수 있어야 한다.
- 지출 카테고리는 사용자가 직접 관리하며 추가, 삭제, 변경이 가능해야 한다.
- 카테고리 이름이나 색상을 변경하면 같은 카테고리에 연결된 과거 지출 내역의 표시에도 변경된 값이 적용된다.
- 카테고리를 삭제하더라도 기존 지출 내역이 깨지지 않아야 한다. 연결된 내역이 있는 카테고리는 사용 중지/숨김 처리하고, 연결된 내역이 없는 카테고리만 완전 삭제를 허용한다.

### 결제수단/계좌
- 결제방법에는 현금과 사용자가 등록하는 카드가 추가된다.
- 카드 등록시에는 카드이름, 카드사, 결제일을 사용자가 입력하고 결제계좌를 등록한 통장계좌 중 선택한다.
- 카드 결제일은 카드 청구 관리를 위한 참고 정보로 저장한다. 예산 차감과 잔액 반영은 결제 입력 시점 기준으로 처리하고, 실제 카드값 출금 예정액 계산은 MVP 이후 기능으로 분리한다.
- MVP 이후 카드값 출금 예정액 기능을 추가할 때는 카드별 결제일뿐 아니라 결제금액 산정 기간도 함께 고려한다.
- 카드 삭제 시 기존 지출 내역이 깨지지 않아야 한다. 연결된 내역이 있는 카드는 사용 중지/숨김 처리하고, 연결된 내역이 없는 카드만 완전 삭제를 허용한다.
- 통장계좌는 사용자가 직접 관리하며 추가, 삭제, 변경이 가능해야 한다.
- 통장계좌 등록시에는 계좌명, 은행명, 계좌번호, 잔액을 사용자가 입력하고 현재잔액 포함여부를 선택한다.
- 사용자가 입력한 잔액은 현재 시점의 기준 잔액으로 저장하고, 이후 앱에 기록한 수입/지출을 반영해 계산한다.
- 이 앱에서 말하는 `잔액`은 실제 은행 잔액과의 일치를 보장하는 값이 아니라, 예산 범위 안에서 사용할 수 있는 앱 내부 가용 잔액을 의미한다.
- 현재잔액 포함여부는 홈 화면의 현재 총잔액에 해당 계좌의 잔액을 포함할지 여부를 의미한다.
- 사용자는 계좌 잔액을 수동으로 보정할 수 있어야 하며, 보정 내역은 잔액 변경 이력에 남긴다.
- 현금은 결제수단이면서 잔액을 가지므로 내부적으로 `현금 계좌` 형태의 Account로 모델링한다.
- 앱 최초 실행 시 기본 현금 계좌와 현금 결제수단을 생성한다.
- 지출 입력 시 선택한 결제수단에 연결된 통장계좌 또는 현금 계좌의 잔액을 자동으로 차감한다.
- 수입 입력 시 선택한 통장계좌 또는 현금 계좌의 잔액을 자동으로 증가시킨다.
- 통장계좌 삭제 시 기존 수입/지출 내역이 깨지지 않아야 한다. 연결된 내역이 있는 계좌는 사용 중지/숨김 처리하고, 연결된 내역이 없는 계좌만 완전 삭제를 허용한다.

### 보기/알림
- 하단 탭은 `홈(현황)`, `달력`, `보고서`, `관리` 구조로 제공한다.
- 지출/수입 기록은 별도 탭으로 두지 않고 어느 화면에서도 접근 가능한 전역 빠른 기록 버튼으로 제공한다. 버튼을 누르면 지출/수입을 선택하는 하단 시트가 열리고, 선택 후 입력 폼으로 이어진다.
- 기본 보기인 홈 화면은 현재 총잔액(현재잔액 포함여부가 켜진 모든 계좌의 잔액 합계), 카테고리별 예산 대비 지출금액을 막대그래프와 %수치로 제공한다. 이 때 %수치는 손으로 탭하면 실제 금액으로 변경된다.
- 별도 탭으로 달력보기 화면을 제공해 일별 지출금액을 한 눈에 확인할 수 있어야 한다.
- 보고서 화면에서는 월별 총지출, 카테고리별 예산 대비 사용률, 남은 예산, 일별 지출 추이, 결제수단별 지출을 제공한다.
- 연단위로 지출 추이를 확인할 수 있도록 월별 카테고리별 지출 추이 그래프를 MVP에 포함한다.
- 알림은 카테고리별 예산 대비 50%, 80%, 100% 사용 시에만 경고성으로 제공한다.
- 예산 경고 알림은 기준선을 넘는 순간 카테고리별/예산기간별/기준선별 1회 발송한다. 지출 수정이나 환불로 사용률이 기준선 아래로 내려가면 해당 기준선 알림 상태를 초기화하고, 다시 기준선을 넘는 경우에는 재알림을 허용한다.

### 백업/보안
- 데이터는 기본적으로 로컬 기기에만 저장한다.
- 기기 분실/교체에 대비해 수동 내보내기와 가져오기 기능을 제공한다.
- JSON은 전체 백업/복원용 형식으로 사용하고, CSV는 지출/수입 내역을 외부에서 확인하기 위한 분석용 내보내기 형식으로 사용한다.
- 가져오기는 MVP에서 전체 복원 방식으로 제공한다. 가져오기 실행 전 기존 데이터가 대체된다는 확인 화면을 제공하고, 병합 가져오기는 MVP 이후 기능으로 분리한다.
- 내보내기 전에는 민감 정보가 포함된다는 확인 화면을 제공한다.
- 기본 내보내기에는 삭제된 항목을 포함하지 않는다. 가져오기/복원 시 외부 파일에 삭제 상태 데이터가 포함되어 있으면 삭제 상태를 유지해 복원한다.
- 백업 파일에는 계좌번호 전체를 포함한다.
- 금액, 계좌번호 등 민감한 정보가 포함되므로 앱 화면에서는 계좌번호를 일부 마스킹한다.
- MVP에는 빠른 입력 흐름을 해치지 않는 간단한 비밀번호 또는 패턴 잠금 기능을 포함한다. 잠금 비밀번호와 패턴 원본은 저장하지 않고 검증 가능한 형태로만 저장한다.
- 생체 인증과 백업 파일 암호화는 MVP 이후 기능으로 검토한다.

## 시스템 아키텍처
- MVP는 백엔드 서버 없이 Android와 iOS에서 동작하는 Flutter 기반 로컬 우선 앱으로 만든다. 카드사/은행 연동, 계정 로그인, 클라우드 동기화는 초기 범위에서 제외한다.
- 하나의 Flutter 코드베이스를 Presentation, Application, Domain, Data 계층으로 나누는 계층형 아키텍처를 사용한다. Android와 iOS의 플랫폼별 코드는 알림, 보안 저장소, 파일 공유처럼 네이티브 기능 연결이 필요한 최소 범위로 제한한다.
    - Presentation Layer: Flutter 화면과 공통 위젯, Material 3 테마, 내비게이션, 사용자 입력 및 화면 상태 표시를 담당한다.
    - Application Layer: Riverpod Provider/Notifier를 통해 UI 상태와 사용자 이벤트를 관리하고 UseCase 실행 결과를 화면에 전달한다.
    - Domain Layer: 지출/수입 등록, 환불/취소 처리, 예산 차감, 계좌 잔액 반영, 월별/연간 보고서 계산, 카테고리별 사용률 계산, 알림 발송 이력 관리 같은 핵심 규칙을 순수 Dart 모델과 UseCase로 분리한다.
    - Data Layer: Repository 구현체, SQLite DataSource, 설정 저장소, 알림 및 백업/복원 서비스를 담당한다.
- 데이터 흐름은 `사용자 입력 -> Widget -> Provider/Notifier -> UseCase -> Repository -> Local DB -> Provider 상태 갱신 -> Widget 재빌드`의 단방향 흐름으로 구성한다.
- 로컬 데이터 저장은 구조화된 지출/카테고리/결제수단/계좌/월예산 데이터를 다루므로 `sqflite`를 통한 SQLite를 사용한다. SQL 스키마 버전과 마이그레이션을 명시적으로 관리한다.
- 앱 설정, 기본 결제수단, 알림 시간, 앱 잠금 사용 여부처럼 작은 key-value 데이터는 `shared_preferences`에 저장한다. 비밀번호 검증값이나 보안이 필요한 값은 일반 설정과 분리해 `flutter_secure_storage`에 저장한다.
- 금액은 소수점 오차를 피하기 위해 Dart `int` 타입의 KRW 정수 금액으로 저장한다. JSON 백업에서도 정수로 직렬화한다.
- 주요 데이터 모델은 다음과 같이 나눈다.
    - Expense: 지출일시, 금액, 카테고리, 결제수단, 메모, 환불/취소 여부, 원 지출 참조(`refundedExpenseId`), 생성/수정/삭제일
    - Income: 수입일시, 금액, 입금 대상 계좌 또는 현금, 메모, 생성/수정/삭제일
    - Category: 이름, 색상, 정렬순서, 사용 여부
    - BudgetPeriod: 시작일, 종료일, 표시 연월, 월 시작일 기준값
    - MonthlyBudget: 예산기간, 카테고리, 예산금액
    - PaymentMethod: 현금/카드 구분, 카드명, 카드사, 결제일, 연결 계좌, 사용 여부
    - Account: 계좌 유형(통장/현금), 계좌명, 은행명, 계좌번호(화면 표시 시 마스킹), 잔액, 현재잔액 포함여부, 사용 여부
    - AccountLedger: 지출/수입/수동 보정으로 인한 잔액 변경 이력, 원천 타입, 원천 id, 변경금액, 변경 후 잔액
    - ExpenseTemplate: 자주 쓰는 지출명, 기본 금액, 기본 카테고리, 기본 결제수단
    - AlertHistory: 예산기간, 카테고리, 기준선, 발송일시, 재알림 판단 상태
- 화면 구성은 `홈(현황)`, `달력`, `보고서`, `관리`를 기본 탭으로 설계하고, `빠른 지출 입력`과 `수입 입력`은 모든 주요 화면에서 열 수 있는 하단 시트 또는 모달로 제공한다.
- 지출/수입/환불/삭제/잔액보정은 Account 잔액과 AccountLedger가 서로 어긋나지 않도록 하나의 SQLite transaction 안에서 처리한다.
- 카테고리별 예산 50%, 80%, 100% 도달 여부는 거래 저장 트랜잭션 직후 계산하고, 로컬 알림은 `flutter_local_notifications`로 제공한다. 삭제 데이터 2개월 후 정리는 앱 시작 및 데이터 변경 시점에 우선 실행하며, 앱이 장기간 실행되지 않아도 다음 실행 시 정리되도록 한다. OS 백그라운드 실행이 꼭 필요해질 때만 `workmanager` 도입을 검토한다.
- 보고서 계산은 DB에 저장된 원천 지출 데이터를 기준으로 매번 계산하거나 필요한 집계만 캐싱한다. 초기에는 실시간 계산으로 시작하고 데이터가 많아졌을 때 월별/연간 집계 테이블을 추가한다.
- 조회, 보고서, CSV 내보내기에서는 `deletedAt`이 있는 삭제 데이터를 기본적으로 제외한다.
- DB 마이그레이션과 백업/복원을 염두에 두고 모든 주요 테이블에는 `id`, `createdAt`, `updatedAt`, `isArchived` 또는 `deletedAt` 필드를 둔다.
- Android와 iOS가 같은 데이터를 해석할 수 있도록 날짜/시간은 DB와 백업 파일에 ISO 8601 문자열로 저장하고, 금액·식별자·스키마 버전의 직렬화 규칙을 플랫폼과 무관하게 고정한다.
- 플랫폼별 권한과 기능 차이는 서비스 인터페이스 뒤로 격리한다. 알림 권한은 Android 13 이상과 iOS에서 런타임에 요청하고, 파일 내보내기/가져오기와 앱 잠금은 각 플랫폼의 동작을 실제 기기에서 검증한다.
- iOS 빌드와 배포는 macOS와 Xcode가 필요하다. Windows에서는 공통 Flutter 코드와 Android 앱을 개발하고 테스트하며, iOS 빌드 검증은 macOS 환경 또는 CI에서 수행한다.
- 테스트는 예산기간 계산, 예산 차감, 환불/취소, 잔액 반영, 월별/연간 집계, 카테고리 삭제 정책, 알림 발송 이력 같은 도메인 로직의 Dart 단위 테스트를 우선 작성한다. SQLite Repository는 통합 테스트, 주요 화면은 Flutter 위젯 테스트, 핵심 입력·백업 흐름은 Android/iOS 실제 기기 또는 에뮬레이터 통합 테스트를 작성한다.

## 기술 스택
- 언어/플랫폼: Dart, Flutter stable, Android, iOS
- UI: Flutter Material 3 위젯, `ThemeData`, `ColorScheme`, `TextTheme`
- 내비게이션: 기본 탭은 `NavigationBar`와 `IndexedStack`, 입력·상세·설정 하위 경로는 `go_router`
- 상태 관리 및 의존성 연결: Riverpod. 비동기 데이터는 `AsyncValue`로 로딩/오류/데이터 상태를 명시한다.
- 아키텍처: 계층형 아키텍처 + Repository Pattern + 필요한 범위의 UseCase. 폴더는 `presentation`/`application`/`domain`/`data` 책임을 기준으로 나눈다.
- 로컬 DB: `sqflite`, SQLite, 명시적 스키마 버전 및 migration
- 설정 저장: `shared_preferences`
- 민감 정보 저장: `flutter_secure_storage`를 통해 Android Keystore와 iOS Keychain 사용
- 로컬 알림: `flutter_local_notifications`. OS 백그라운드 작업이 필수인 요구가 생기면 `workmanager`를 추가 검토한다.
- 날짜/시간 및 다국어: Dart `DateTime`, `intl`, `flutter_localizations`. 화면과 데이터 경계에서 로컬 시간대와 ISO 8601 변환을 명시한다.
- 백업/복원: `dart:convert` 기반 버전 명시 JSON 백업/복원, CSV Export, `path_provider`와 플랫폼 파일 선택/공유 플러그인
- 차트/리포트 UI: 초기 막대와 진행률은 Flutter 기본 위젯/`CustomPainter`로 구현하고, 접근성과 상호작용이 복잡해질 때 안정 버전의 Flutter 차트 라이브러리를 검토한다.
- 테스트: `flutter_test`, Dart 단위 테스트, Flutter 위젯 테스트, `integration_test`, SQLite Repository 통합 테스트
- 정적 분석/포맷: `flutter_lints`, `flutter analyze`, `dart format`
- 빌드/개발 도구: Flutter CLI, Android Studio 최신 stable 및 Android SDK, iOS 빌드용 macOS/Xcode, 라이브러리는 stable 버전 우선
