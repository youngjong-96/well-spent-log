# 디자인 가이드

## 디자인 목표
잘쓸결심은 개인이 매일 빠르게 지출과 수입을 기록하고, 월별 예산 상태를 또렷하게 확인하기 위한 Android/iOS 모바일 앱이다. 하나의 Flutter UI를 기반으로 두 플랫폼에서 같은 정보 구조와 조작 흐름을 제공한다. 디자인은 화려한 금융 대시보드보다 조용하고 빠른 기록 도구에 가깝게 만든다.

- 빠른 기록: 지출 발생 직후 10초 안에 입력할 수 있어야 한다.
- 명확한 자각: 오늘 얼마를 썼고, 이번 달 예산이 얼마나 남았는지 바로 보여야 한다.
- 부담 없는 관리: 절약을 강요하기보다 사용자가 직접 판단할 수 있게 정보를 정리한다.
- 일관된 조작: 같은 종류의 입력, 선택, 삭제, 저장 흐름은 모든 화면에서 같은 모양과 위치를 유지한다.

## 제품 톤
- 차분하고 실용적인 개인 재무 도구 느낌을 유지한다.
- 문구는 짧고 직접적으로 쓴다. 예: `지출 추가`, `수입 추가`, `예산 80% 사용`, `삭제할까요?`
- 경고 문구는 과장하지 않는다. 예산 초과도 비난이 아니라 상태 안내로 표현한다.
- 화면에서 `잔액`은 앱에 기록된 수입/지출을 반영한 내부 가용 잔액을 뜻한다. 실제 은행 잔액과 일치한다고 오해하게 만드는 문구는 피한다.
- 장식보다 정보의 위계와 여백을 우선하며, 색상은 제한된 파스텔 블루 팔레트 안에서만 사용한다.

## 화면 구조
- 하단 탭은 `홈`, `달력`, `보고서`, `관리` 4개를 기본으로 한다.
- 지출/수입 기록은 독립 탭으로 두지 않고 모든 주요 화면에서 열 수 있는 전역 빠른 기록 버튼으로 제공한다.
- 전역 빠른 기록 버튼을 누르면 `지출`, `수입`을 선택하는 하단 시트를 먼저 보여주고, 선택 후 해당 입력 폼으로 이동한다.
- `홈` 화면은 이번 달 지출 합계와 카테고리별 예산 사용 현황을 첫 화면으로 보여준다.
- `달력` 화면은 월 이동, 일별 총지출, 카테고리 색상 점, 선택일 내역을 보여준다.
- `보고서` 화면은 월별 총지출, 남은 예산, 카테고리별 사용률, 일별 추이, 결제수단별 지출, 연간 카테고리별 지출 추이를 보여준다.
- `관리` 화면에는 카테고리/예산, 결제수단/계좌, 템플릿, 백업, 잠금 설정을 배치한다.

## 색상
Material 3를 기준으로 흰색과 파스텔 블루 계열만 사용한다. 앱 전체의 기본색은 아래 5개로 제한하며, 화면별 임의 색상은 추가하지 않는다. 투명도는 계층과 상태를 보조할 때만 사용한다.

- White / Surface: `#FFFFFF`
- Pale Blue / Background: `#F2F7FC`
- Soft Blue / Container, Border: `#D8E8F5`
- Sky Blue / Primary, Accent: `#84B6E2`
- Navy Blue / Text, Strong State: `#294761`

카테고리는 `Soft Blue`, `Sky Blue`, `Navy Blue`를 순환해서 사용한다. 기존 데이터에 다른 색상이 저장되어 있어도 화면에서는 이 세 색 중 하나로 정규화한다. 카테고리명과 아이콘을 항상 함께 표시하여 색상만으로 의미를 구분하지 않는다.

수입, 경고, 예산 초과에도 별도의 초록·노랑·주황·빨강을 추가하지 않는다. 수입은 `+` 부호와 라벨로, 경고 단계는 퍼센트와 굵기 및 `Sky Blue`에서 `Navy Blue`로 깊어지는 명도로 전달한다.

## 타이포그래피
- 기본 폰트는 부드럽고 친근한 인상의 `고운돋움`을 앱 자산으로 포함해 Android와 iOS에 동일하게 적용한다. 아이콘은 별도의 `MaterialIcons` 글꼴을 사용해 본문 글꼴의 영향을 받지 않게 한다.
- 아래 글자 크기는 Flutter `TextStyle.fontSize`의 논리 픽셀 기준이며 시스템 텍스트 배율에 따라 확대된다.
- 숫자는 금액 식별이 중요하므로 굵기와 자릿수 구분을 명확히 한다.
- 화면 제목: 22, Semibold
- 섹션 제목: 16, Semibold
- 본문: 14 또는 15, Regular
- 보조 설명: 12 또는 13, Regular
- 주요 금액: 24~32, Bold
- 버튼/칩: 14, Medium

## 여백과 형태
- 크기와 간격은 Flutter 논리 픽셀 기준으로 정의한다.
- 기본 간격은 4 단위 그리드를 따른다.
- 화면 좌우 여백은 16을 기본으로 한다.
- 리스트 항목 높이는 최소 56을 유지한다.
- 터치 대상은 최소 48을 유지한다.
- 카드와 입력 필드의 모서리는 8 이하로 통일한다.
- 달력 셀, 아이콘 버튼, 금액 입력 키패드는 고정 크기를 사용해 상태 변화로 레이아웃이 흔들리지 않게 한다.

## 컴포넌트 원칙
- 버튼은 명확한 명령에만 사용한다. 반복 도구는 가능한 아이콘 버튼을 사용하고 툴팁 또는 접근성 라벨을 제공한다.
- 전역 빠른 기록 버튼은 모든 주요 화면에서 같은 위치와 같은 아이콘으로 제공한다.
- 카테고리는 색상 스와치 + 이름 조합으로 표시한다.
- 결제수단은 현금/카드 아이콘과 이름을 함께 표시한다.
- 예산 사용률은 진행 막대와 퍼센트를 함께 보여준다.
- 경고 상태는 색상만으로 전달하지 않고 `50%`, `80%`, `초과` 텍스트를 함께 표시한다.
- 삭제는 즉시 실행하지 않고 확인 다이얼로그를 거친다.
- 저장 직후에는 스낵바로 `저장했어요`와 `되돌리기`를 제공한다.
- 백업 내보내기와 가져오기는 민감 정보 포함 또는 기존 데이터 대체 여부를 명확히 알리는 확인 화면을 거친다.

## 입력 UX
- 지출/수입 입력은 어느 화면에서나 전역 빠른 기록 버튼으로 열 수 있어야 한다.
- 지출 입력은 기본값을 최대한 채운다: 오늘 날짜, 최근 결제수단, 최근 카테고리.
- 금액 입력은 숫자 키패드를 먼저 보여주고, 카테고리/결제수단/날짜는 한 손 조작이 쉬운 순서로 배치한다.
- 최근 입력 항목은 금액, 카테고리, 결제수단을 한 번에 불러올 수 있어야 한다.
- 즐겨찾기 템플릿은 칩 또는 작은 리스트로 제공하고, 누르면 즉시 입력 폼에 반영한다.
- 지출과 수입은 같은 입력 패턴을 공유하되 색상과 라벨로 명확히 구분한다.
- 환불/취소 입력에서도 사용자는 양수 금액을 입력한다. 화면에는 `환불로 기록` 같은 라벨과 함께 저장 후 지출에서 차감된다는 점을 보여준다.
- 원 지출과 연결하는 환불/취소 흐름은 최근 지출 검색 또는 선택 UI를 제공하되, 연결 없이 저장하는 선택지도 제공한다.

## 주요 화면 가이드
- 홈: 이번 달 지출 합계, 카테고리별 예산 사용 막대, 이번 달 지출 내역, 빠른 기록 버튼을 순서대로 보여준다.
- 이번 달 지출 내역은 금액이 큰 순으로 정렬하고 10개씩 페이지를 나눠 표시한다.
- 빠른 기록: 금액 입력 영역을 가장 크게 두고, 카테고리와 결제수단 선택은 바로 아래에 둔다.
- 달력: 각 날짜에는 일별 총지출과 최대 3개의 카테고리 점을 표시한다. 선택한 날짜의 내역은 하단 영역에 표시한다.
- 보고서: 월간 현황을 기본으로 보여주고, 연간 카테고리별 추이 그래프는 별도 모드나 필터로 제공한다.
- 관리: 설정 항목을 긴 카드 묶음으로 만들지 말고, 리스트와 섹션 헤더 중심으로 구성한다.
- 잠금 화면: 빠르게 해제할 수 있도록 숫자 비밀번호 또는 패턴 입력을 단순하게 제공한다.

## 차트와 상태 표현
- 예산 사용률은 0~49%는 카테고리 블루, 50~79%는 `Sky Blue`, 80% 이상은 `Navy Blue`로 표시한다. `80%`, `초과` 같은 텍스트를 함께 제공한다.
- 수입은 `Sky Blue`와 `+` 부호, 수입 라벨을 함께 사용한다.
- 수입은 색상만으로 구분하지 않고 `+` 부호, 수입 라벨, 아이콘을 함께 사용한다.
- 보고서 차트는 막대 차트를 우선 사용한다. 도넛 차트는 카테고리 비중을 보여줄 때만 사용한다.
- 결제수단별 지출은 작은 막대 차트나 정렬된 리스트로 보여준다.
- 연간 카테고리별 지출 추이는 월별 변화를 비교하기 쉬운 선형 또는 누적 막대 차트로 표시한다.
- 차트에는 숫자 라벨을 함께 제공해 색상 구분 없이도 의미를 알 수 있게 한다.

## 접근성
- 모든 텍스트와 배경은 충분한 대비를 유지한다.
- 색상만으로 상태를 전달하지 않는다.
- 화면 읽기 도구를 위해 버튼, 아이콘, 금액, 진행률에는 명확한 접근성 라벨을 붙인다.
- 작은 금액/날짜 텍스트도 기본 크기 12 아래로 내리지 않는다.
- 중요한 확인 버튼과 삭제 버튼은 충분히 떨어뜨려 오동작을 줄인다.

## Flutter 적용 기준
- `ThemeData.colorScheme`에 위 색상 토큰을 매핑하고 `MaterialApp`에서 앱 전역 테마로 적용한다.
- `TextTheme`은 Material 3 타입 스케일을 기반으로 앱 전역에서만 정의한다.
- 공통 컴포넌트는 `AppButton`, `AmountInput`, `QuickEntryFab`, `TransactionTypeSheet`, `CategoryChip`, `PaymentMethodChip`, `BudgetProgress`, `TransactionListItem`, `MonthCalendar`처럼 재사용 가능한 단위로 만든다.
- 화면별로 임의 색상과 임의 여백을 만들지 않고 디자인 토큰을 사용한다.
- Android와 iOS에서 같은 Material 3 디자인을 유지하되, 뒤로가기 제스처, 키보드, 날짜 선택, 안전 영역처럼 플랫폼 사용성이 중요한 동작은 Flutter의 플랫폼 적응형 동작을 따른다.
- 모든 화면은 `SafeArea`, 키보드 인셋, 작은 화면과 큰 글자 배율을 고려한다. 360 논리 픽셀 너비와 텍스트 배율 200%에서도 버튼 및 금액 텍스트가 잘리거나 겹치지 않아야 한다.
- 접근성 라벨은 `Semantics`와 위젯의 `tooltip`을 사용하고, 포커스 순서와 스크린 리더 읽기 순서를 위젯 테스트 및 실제 기기에서 확인한다.
- 다크 모드는 MVP 이후로 두되, 색상 토큰 이름은 다크 모드 확장이 가능하도록 유지한다.
